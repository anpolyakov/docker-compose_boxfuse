version: "3"
services: 
    maven:
        image: maven:3.3-jdk-8
        container_name: maven
        volumes: 
            - ${PWD}:/usr/src/mymaven/
        working_dir: /usr/src/mymaven/
        command: mvn verify
        links:
            - tomcat

    tomcat:
        image: tomcat:latest
        container_name: tomcat
        ports: 
            - "8080:8080"

    